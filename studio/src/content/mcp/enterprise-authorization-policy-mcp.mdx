---
name: "Enterprise Authorization Policy MCP"
name_en: "Enterprise Authorization Policy MCP"
name_zh: "企业授权策略 MCP"
description: "把企业 SSO/IAM 策略映射到 MCP Server 的工具 scope 和审批规则。"
source: "FlowBase 原创"
category: ["security", "auth"]
tags: ["authorization", "iam", "sso"]
platform: ["claude-code", "codex"]
github_url: "https://github.com/openfga/openfga"
official_url: "https://openfga.dev/"
install_cmd: "npx -y enterprise-authz-mcp --readonly"
config_snippet:
  mcpServers:
    enterpriseAuthzPolicy:
      command: "npx"
      args: ["-y","enterprise-authz-mcp","--readonly"]
is_featured: true
order: 103
author: "FlowBase"
updated_at: "2026-07-25"
---

## 这个 MCP 能做什么？

企业授权策略 MCP 面向 Claude Code 中文工作台，帮助团队把 企业授权策略 接入可选择、可配置、可导出的 Agent 工作流。

## 国内团队使用场景

- 企业项目、云资源、AI 搜索、测试和 Agent 治理场景。
- 需要把外部系统变成 Claude Code 可控工具源。
- 需要在导出前明确权限、参数和审计边界。

## 配置示例

```json
{
  "mcpServers": {
    "enterpriseAuthzPolicy": {
      "command": "npx",
      "args": [
      "-y",
      "enterprise-authz-mcp",
      "--readonly"
]
    }
  }
}
```

## 使用建议

- 先启用只读模式。
- 为高危写入动作加人工确认。
- 每次导出后记录版本、负责人和验证结果。

