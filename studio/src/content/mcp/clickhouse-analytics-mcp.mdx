---
name: "ClickHouse Analytics MCP"
name_en: "ClickHouse Analytics MCP"
name_zh: "ClickHouse 实时分析 MCP"
description: "把 ClickHouse 查询能力接入 Agent，用于日志、增长和业务指标分析。"
source: "FlowBase 原创"
category: ["database", "analytics"]
tags: ["clickhouse", "analytics", "sql"]
platform: ["claude-code", "codex"]
github_url: "https://github.com/ClickHouse/clickhouse-js"
official_url: "https://clickhouse.com/docs"
install_cmd: "npx -y clickhouse-mcp-server --readonly"
config_snippet:
  mcpServers:
    clickhouseAnalytics:
      command: "npx"
      args: ["-y", "clickhouse-mcp-server", "--readonly"]
is_featured: true
order: 77
author: "FlowBase"
updated_at: "2026-06-12"
---

## 这个 MCP 能做什么？

ClickHouse 实时分析 MCP 是面向 Claude Code Workspace 的接入蓝图，用于把 ClickHouse 实时分析库 纳入可选择、可配置、可导出的工作台能力。

## 国内团队使用场景

- 在 FlowBase Studio 中作为项目上下文或自动化工具源。
- 连接企业内部知识、工单、数据、云资源或分析系统。
- 为 AI Agent 任务保留权限、参数和审计边界。

## 配置示例

```json
{
  "mcpServers": {
    "clickhouseAnalytics": {
      "command": "npx",
      "args": [
      "-y",
      "clickhouse-mcp-server",
      "--readonly"
]
    }
  }
}
```

## 使用建议

- 先在测试空间连接，不直接使用生产密钥。
- 只开放最小读取或写入权限。
- 对删除、发送、支付、发布类动作增加人工确认。

