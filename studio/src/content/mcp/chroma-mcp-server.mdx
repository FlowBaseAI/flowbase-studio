---
name: "Chroma MCP Server"
name_en: "Chroma MCP Server"
name_zh: "Chroma 向量数据库 MCP"
description: "让 Agent 读写 Chroma 集合，适合本地知识库和轻量 RAG。"
source: "Chroma"
category: ["vector-db","rag"]
tags: ["chroma","vector","rag","knowledge-base"]
platform: ["claude-code","codex"]
github_url: "https://github.com/chroma-core/chroma-mcp"
official_url: "https://github.com/chroma-core/chroma-mcp"
install_cmd: "uvx chroma-mcp"
config_snippet:
  mcpServers:
    chroma:
      command: "uvx"
      args: ["chroma-mcp"]
is_featured: false
order: 55
author: "Chroma"
updated_at: "2026-06-11"
---

## 这个 MCP 能做什么？

Chroma 向量数据库 MCP 适合把 vector-db 能力接入 Claude Code Workspace，为国内团队提供可选、可配置、可导出的 MCP 工具。

## 国内团队使用场景

- 作为 FlowBase Studio Workspace 数据源
- 服务 AI Agent 的安全、观测、RAG、搜索或身份场景
- 为国内团队提供配置示例和权限边界

## 配置示例

```json
{
  "mcpServers": {
    "chroma": {
      "command": "uvx",
      "args": [
      "chroma-mcp"
]
    }
  }
}
```

## 使用建议

- 先在测试环境连接。
- 只开放最小权限。
- 涉及身份、日志或数据检索时保留审计记录。
