---
name: "MCP Tasks Runner Server"
name_en: "MCP Tasks Runner Server"
name_zh: "MCP Tasks 长任务 MCP"
description: "把长任务、后台执行和状态查询封装成 Claude Code 可追踪工具。"
source: "FlowBase 原创"
category: ["mcp", "automation"]
tags: ["tasks", "long-running", "workflow"]
platform: ["claude-code", "codex"]
github_url: "https://github.com/modelcontextprotocol/python-sdk"
official_url: "https://modelcontextprotocol.io/"
install_cmd: "uvx mcp-tasks-runner"
config_snippet:
  mcpServers:
    mcpTasksRunner:
      command: "uvx"
      args: ["mcp-tasks-runner"]
is_featured: false
order: 102
author: "FlowBase"
updated_at: "2026-07-25"
---

## 这个 MCP 能做什么？

MCP Tasks 长任务 MCP 面向 Claude Code 中文工作台，帮助团队把 MCP Tasks 长任务执行 接入可选择、可配置、可导出的 Agent 工作流。

## 国内团队使用场景

- 企业项目、云资源、AI 搜索、测试和 Agent 治理场景。
- 需要把外部系统变成 Claude Code 可控工具源。
- 需要在导出前明确权限、参数和审计边界。

## 配置示例

```json
{
  "mcpServers": {
    "mcpTasksRunner": {
      "command": "uvx",
      "args": [
      "mcp-tasks-runner"
]
    }
  }
}
```

## 使用建议

- 先启用只读模式。
- 为高危写入动作加人工确认。
- 每次导出后记录版本、负责人和验证结果。

