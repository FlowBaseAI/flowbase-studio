---
name: "Terraform MCP Server"
name_en: "Terraform MCP Server"
name_zh: "Terraform 基础设施 MCP"
description: "让 Agent 查询 Terraform Provider、模块和基础设施配置，适合 IaC 审查。"
source: "HashiCorp"
category: ["infrastructure","devops"]
tags: ["terraform","iac","devops","cloud"]
platform: ["claude-code","codex"]
github_url: "https://github.com/hashicorp/terraform-mcp-server"
official_url: "https://github.com/hashicorp/terraform-mcp-server"
install_cmd: "docker run --rm hashicorp/terraform-mcp-server"
config_snippet:
  mcpServers:
    terraform:
      command: "docker"
      args: ["run","--rm","hashicorp/terraform-mcp-server"]
is_featured: true
order: 31
author: "HashiCorp"
updated_at: "2026-06-10"
---

## 这个 MCP 能做什么？

Terraform 基础设施 MCP 适合把 infrastructure 能力接入 Claude Code Workspace，让国内团队在选择 MCP 时更容易判断用途、权限和验证方法。

## 国内团队使用场景

- 作为 Studio Workspace 的 MCP 数据源
- 为 Claude Code 或 Codex 提供外部工具能力
- 服务国内团队的开发、运维、安全或内容工作流

## 配置示例

```json
{
  "mcpServers": {
    "terraform": {
      "command": "docker",
      "args": [
      "run",
      "--rm",
      "hashicorp/terraform-mcp-server"
]
    }
  }
}
```

## 使用建议

- 先在测试项目启用。
- 检查安装命令和权限范围。
- 涉及云资源或安全扫描时必须保留审计记录。
