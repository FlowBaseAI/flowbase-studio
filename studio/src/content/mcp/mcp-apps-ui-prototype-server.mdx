---
name: "MCP Apps UI Prototype Server"
name_en: "MCP Apps UI Prototype Server"
name_zh: "MCP Apps UI 原型 MCP"
description: "为 MCP Apps server-rendered UI 生成交互原型和权限说明。"
source: "FlowBase 原创"
category: ["mcp", "ui"]
tags: ["mcp-apps", "ui", "prototype"]
platform: ["claude-code", "codex"]
github_url: "https://github.com/modelcontextprotocol/typescript-sdk"
official_url: "https://modelcontextprotocol.io/"
install_cmd: "npx -y mcp-apps-ui-prototype"
config_snippet:
  mcpServers:
    mcpAppsUiPrototype:
      command: "npx"
      args: ["-y","mcp-apps-ui-prototype"]
is_featured: true
order: 101
author: "FlowBase"
updated_at: "2026-07-25"
---

## 这个 MCP 能做什么？

MCP Apps UI 原型 MCP 面向 Claude Code 中文工作台，帮助团队把 MCP Apps UI 原型 接入可选择、可配置、可导出的 Agent 工作流。

## 国内团队使用场景

- 企业项目、云资源、AI 搜索、测试和 Agent 治理场景。
- 需要把外部系统变成 Claude Code 可控工具源。
- 需要在导出前明确权限、参数和审计边界。

## 配置示例

```json
{
  "mcpServers": {
    "mcpAppsUiPrototype": {
      "command": "npx",
      "args": [
      "-y",
      "mcp-apps-ui-prototype"
]
    }
  }
}
```

## 使用建议

- 先启用只读模式。
- 为高危写入动作加人工确认。
- 每次导出后记录版本、负责人和验证结果。

