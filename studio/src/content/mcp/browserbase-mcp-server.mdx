---
name: "Browserbase MCP Server"
name_en: "Browserbase MCP Server"
name_zh: "Browserbase 云浏览器 MCP"
description: "为 Agent 提供云浏览器会话，适合动态页面测试、登录态自动化和浏览器任务。"
source: "Browserbase"
category: ["browser","automation"]
tags: ["browserbase","browser","automation","cloud"]
platform: ["claude-code","codex"]
github_url: "https://github.com/browserbase/mcp-server-browserbase"
official_url: "https://github.com/browserbase/mcp-server-browserbase"
install_cmd: "npx -y @browserbase/mcp-server-browserbase"
config_snippet:
  mcpServers:
    browserbase:
      command: "npx"
      args: ["-y","@browserbase/mcp-server-browserbase"]
is_featured: false
order: 17
author: "Browserbase"
updated_at: "2026-06-09"
---

## 这个 MCP 能做什么？

Browserbase 云浏览器 MCP 能把 browserbase 相关能力接入 Claude Code Workspace，让国内团队在一个可视化工作台中选择、配置并导出项目。它适合与 GitHub、Filesystem、Playwright 和 SEO 类 Skill 搭配使用。

## 国内团队使用场景

- 为 Claude Code 提供可调用工具上下文
- 把热门国际工具转成国内团队可落地流程
- 在 Studio 中作为 Workspace 数据源选择

## 配置示例

```json
{
  "mcpServers": {
    "browserbase": {
      "command": "npx",
      "args": [
      "-y",
      "@browserbase/mcp-server-browserbase"
]
    }
  }
}
```

## 使用建议

- 先在测试项目中启用，不要直接接入生产环境。
- 只开放当前任务需要的权限和环境变量。
- 记录安装命令、配置片段和验证结果。
