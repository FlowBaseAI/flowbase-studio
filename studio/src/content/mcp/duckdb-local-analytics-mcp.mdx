---
name: "DuckDB Local Analytics MCP"
name_en: "DuckDB Local Analytics MCP"
name_zh: "DuckDB 本地分析 MCP"
description: "让 Claude Code 在本地读取 CSV、Parquet 和轻量数据集并生成可复核 SQL。"
source: "FlowBase 原创"
category: ["database", "analytics"]
tags: ["duckdb", "local-data", "sql"]
platform: ["claude-code", "codex"]
github_url: "https://github.com/duckdb/duckdb-node"
official_url: "https://duckdb.org/docs/"
install_cmd: "npx -y duckdb-mcp-server --sandbox"
config_snippet:
  mcpServers:
    duckdbLocalAnalytics:
      command: "npx"
      args: ["-y", "duckdb-mcp-server", "--sandbox"]
is_featured: true
order: 78
author: "FlowBase"
updated_at: "2026-06-12"
---

## 这个 MCP 能做什么？

DuckDB 本地分析 MCP 是面向 Claude Code Workspace 的接入蓝图，用于把 DuckDB 本地分析环境 纳入可选择、可配置、可导出的工作台能力。

## 国内团队使用场景

- 在 FlowBase Studio 中作为项目上下文或自动化工具源。
- 连接企业内部知识、工单、数据、云资源或分析系统。
- 为 AI Agent 任务保留权限、参数和审计边界。

## 配置示例

```json
{
  "mcpServers": {
    "duckdbLocalAnalytics": {
      "command": "npx",
      "args": [
      "-y",
      "duckdb-mcp-server",
      "--sandbox"
]
    }
  }
}
```

## 使用建议

- 先在测试空间连接，不直接使用生产密钥。
- 只开放最小读取或写入权限。
- 对删除、发送、支付、发布类动作增加人工确认。

